<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- AWDTEAM -->
<mapper namespace="com.ssm.chapter.dao.AwdDao">

	<!-- This code was generated by TableGo tools, mark 1 begin. -->
	<!-- 表所有字段 -->
	<sql id="allColumns">
		ID, TEAMID, EVENTID, FLAG, SOURCE,ipaddr,states
	</sql>
	<!-- This code was generated by TableGo tools, mark 1 end. -->

	<!-- 根据主键查询数据 -->
	<select id="findAwdteamByIds"
		resultType="com.ssm.chapter.pojo.AwdteamEntity" parameterType="int">
		SELECT
		<include refid="allColumns" />
		FROM AWDTEAM WHERE ID IN
		<foreach collection="list" index="index" item="id" open="("
			separator="," close=")">
			#{id}
		</foreach>
	</select>

	<!-- 插入数据 -->
	<insert id="insertAwd"
		parameterType="com.ssm.chapter.pojo.AwdteamEntity">
		INSERT INTO AWDTEAM (
		ID, TEAMID, EVENTID, FLAG,
		SOURCE,ipaddr,states
		)
		VALUES (
		#{id},
		#{teamid},
		#{eventid},
		#{flag},
		#{source},
		#{ipaddr},
		#{states}
		)
	</insert>
	<select id="getawds"
		resultType="com.ssm.chapter.pojo.AwdteamEntity">
		select * from AWDTEAM
	</select>
	<!-- 修改数据 -->
	<update id="updateAwdteam"
		parameterType="com.ssm.chapter.pojo.AwdteamEntity">
		UPDATE AWDTEAM
		<set>
			TEAMID = #{teamid},
			EVENTID = #{eventid},
			FLAG = #{flag},
			SOURCE = #{source},
			ipaddr=#{ipaddr},
			states=#{states}

		</set>
		WHERE ID = #{id}
	</update>
	<select id="getOneawd" parameterType="String"
		resultType="com.ssm.chapter.pojo.AwdteamEntity">
		select * from AWDTEAM where ipaddr=#{ipaddr}
	</select>
	<select id="getOneawdbytemid" parameterType="int"
		resultType="com.ssm.chapter.pojo.AwdteamEntity">
		select * from AWDTEAM where teamid=#{teamid}
	</select>

	<select id="getOneawdbyflag" parameterType="String"
		resultType="com.ssm.chapter.pojo.AwdteamEntity">
		select * from AWDTEAM where flag=#{flag}
	</select>
</mapper>